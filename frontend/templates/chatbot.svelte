<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Importar JSON</title>
    <link rel="stylesheet" href="{{ url_for('serve_css', filename='chatbot.css') }}">
    <link rel="stylesheet" href="{{ url_for('serve_css', filename='home.css') }}">
</head>
<body>
    <div class="container">
        <!-- Menu lateral -->
        <aside class="menu-lateral" aria-label="Menu Lateral">
            <nav>
                <ul class="menu-principal">
                    <li><a href="{{ url_for('home') }}">Dashboard</a></li>
                    <li><a href="#">Upload de Dados</a></li>
                    <li><a href="{{ url_for('chatbot') }}">Chatbot</a></li>
                    <li><a href="{{ url_for('relatorios') }}">Relatórios</a></li>
                </ul>
                <ul class="menu-secundario">
                    <li><a href="{{ url_for('configuracoes') }}">Configurações</a></li>
                    <li><a href="{{ url_for('index') }}">Sobre Nós</a></li>
                    <li><a href="{{ url_for('index') }}">Sair</a></li>
                </ul>
            </nav>
        </aside>

        <!-- Conteúdo principal -->
        <div class="conteudo">
            <header class="cabecalho">
                <a href="{{ url_for('home') }}" class="logo">InsightAI</a>
            </header>

            <main class="principal">
                <section class="titleSearch">
                    <h1>Escreva o que deseja saber sobre os seus dados</h1>
                    <div class="searchBox">
                        <div class="searchWrapper">
                            <input type="text" class="searchJson" placeholder="EX: Quero saber a previsão de vendas para o próximo mês">
                            <button class="sendButton" type="submit">
                                <img src="{{ url_for('serve_images', filename='enviar.png') }}" alt="Enviar" class="sendArrow">
                            </button>
                        </div>
                    </div>
                    <div class="paragrafo">
                        <p> O InsightAI pode cometer erros. Considere verificar informações importantes. </p>
                    </div>
                </section>
            </main>
        </div>
    </div>

    <script src="{{ url_for('serve_js', filename='script.js') }}"></script>
</body>
</html>